    <div><a href="http://localhost:8080/logout"><button type="button" class="btn btn-primary row mb-3">logout</button></a></div>
    
    <div style="display:inline-block; flex-wrap:nowrap">
    {{#each allProds.docs}}
<a href="http://localhost:8080/api/productDetail?product={{this.title}}">
<div class="container">
    <div class= "card" style="width: 18rem;" >
        <img class="card-img-top" src="{{this.image}}" alt="" >
        <h2 class="card-title">{{this.title}}</h2>
        <p class="card-text">{{this.description}}</p>
        <p class="card-text">{{this.category}}</p>
        <a href="http://localhost:8080/api/cart?name={{this.title}}" class="btn btn-primary">Go to cart</a>
    </div>
</div>
</a>
    {{/each}}
    </div>
<br>
<br>
<br>
<div>

    {{#if allProds.hasPrevPage}}
    <a href="{{allProds.prevLink}}">Anterior</a>
    {{/if}}

    |

    {{#if allProds.hasNextPage}}
    <a href="{{allProds.nextLink}}">Siguiente</a>
    {{/if}}

</div>